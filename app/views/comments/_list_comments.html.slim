- @post.comments.each do |comment|
	.comment-user id="#{comment.id}"
		= render partial: "comments/comment_body", locals: {person: comment.author, content: comment.content, data: comment, type: "comment", reply: @reply, comment: comment}
	hr



